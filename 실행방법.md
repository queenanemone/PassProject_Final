# Trip-Board 프로젝트 실행 방법

## 🚀 실행 전 준비사항

### 1. 데이터베이스 설정 (필수)

MySQL 데이터베이스를 먼저 생성해야 합니다:

```sql
CREATE DATABASE travel_planner_korea CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 2. application.yml 설정 확인

`src/main/resources/application.yml` 파일에서 데이터베이스 연결 정보를 확인하세요:

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/travel_planner_korea
    username: ssafy        # 본인의 MySQL 사용자명으로 변경
    password: ssafy       # 본인의 MySQL 비밀번호로 변경
```

---

## 📝 실행 방법

### 방법 1: VS Code에서 실행 (추천)

1. **Java Extension Pack 설치 확인**
   - VS Code에서 `Ctrl+Shift+X`로 확장 프로그램 열기
   - "Extension Pack for Java" 설치 확인

2. **프로젝트 열기**
   - `TripBoardApplication.java` 파일 열기
   - 경로: `src/main/java/com/tripboard/TripBoardApplication.java`

3. **실행**
   - `main` 메서드 위에 있는 **▶ Run** 버튼 클릭
   - 또는 `F5` 키를 눌러 실행
   - 또는 `Ctrl+Shift+P` → "Java: Run Java" 입력

4. **터미널에서 확인**
   - 실행 후 터미널에 "Started TripBoardApplication" 메시지 확인
   - 브라우저에서 `http://localhost:8080` 접속

---

### 방법 2: 터미널에서 Maven으로 실행

**Maven이 설치되어 있는 경우:**

```bash
# 프로젝트 디렉토리로 이동
cd c:\ssafy\passproject

# Maven 빌드
mvn clean install

# Spring Boot 실행
mvn spring-boot:run
```

**Maven이 없는 경우:**
- Maven 설치: https://maven.apache.org/download.cgi
- 또는 VS Code의 Java Extension이 자동으로 Maven을 관리합니다

---

### 방법 3: IDE에서 실행 (IntelliJ, Eclipse 등)

1. 프로젝트를 IDE로 열기
2. `TripBoardApplication.java` 파일 찾기
3. `main` 메서드에서 우클릭 → "Run 'TripBoardApplication'"
4. 또는 `Shift+F10` (IntelliJ) 또는 `Ctrl+F11` (Eclipse)

---

## ✅ 실행 확인

실행이 성공하면:

1. **콘솔 출력 확인:**
   ```
   Started TripBoardApplication in X.XXX seconds
   ```

2. **브라우저에서 접속:**
   - 메인 페이지: http://localhost:8080
   - 로그인 페이지: http://localhost:8080/login.html

3. **API 테스트:**
   - 회원가입: POST http://localhost:8080/api/auth/register
   - 로그인: POST http://localhost:8080/api/auth/login

---

## ⚠️ 문제 해결

### 데이터베이스 연결 오류
- MySQL이 실행 중인지 확인
- `application.yml`의 데이터베이스 정보 확인
- MySQL 포트가 3306인지 확인

### 포트 8080이 이미 사용 중
- `application.yml`에서 포트 변경:
  ```yaml
  server:
    port: 8081  # 다른 포트로 변경
  ```

### Java 버전 오류
- Java 17 이상이 필요합니다
- `java -version`으로 확인

---

## 📞 추가 도움말

문제가 발생하면:
1. VS Code 터미널에서 오류 메시지 확인
2. `application.yml` 설정 확인
3. MySQL 서버 실행 상태 확인

