<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tripboard.mapper.PostLikeMapper">
    
    <resultMap id="PostLikeResultMap" type="com.tripboard.entity.PostLike">
        <id property="postId" column="post_id"/>
        <id property="userId" column="user_id"/>
        <result property="createdAt" column="created_at"/>
    </resultMap>
    
    <select id="findByPostIdAndUserId" resultMap="PostLikeResultMap">
        SELECT * FROM post_likes WHERE post_id = #{postId} AND user_id = #{userId}
    </select>
    
    <insert id="insert">
        INSERT INTO post_likes (post_id, user_id, created_at)
        VALUES (#{postId}, #{userId}, #{createdAt})
    </insert>
    
    <delete id="delete">
        DELETE FROM post_likes WHERE post_id = #{postId} AND user_id = #{userId}
    </delete>
    
</mapper>

